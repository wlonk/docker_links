============
Installation
============

At the command line::

    $ easy_install docker_links

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv docker_links
    $ pip install docker_links